name: "consumer"
version: "1.0.0"
description: "For developers using Baton to manage AI tool configurations in their projects — init, sync, update, and manage workflows."

extends: base

weight: 10

variables:
  default_scope: "project"
