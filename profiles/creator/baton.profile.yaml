name: "creator"
version: "1.0.0"
description: "For developers creating and managing Baton sources and profiles — authoring, publishing, and versioning DX configurations."

extends: base

weight: 10

variables:
  default_transport: "github"
